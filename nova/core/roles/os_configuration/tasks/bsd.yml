---
# - name: Configure OPNsense accounts...
#   ansible.builtin.include_tasks: opnsense.yml # to do
#   when: "'os_opnsense' in group_names"

- name: Configure FreeBSD accounts...
  ansible.builtin.include_tasks: freebsd.yml
  when: "'os_freebsd' in group_names"

# - name: Configure OpenBSD accounts...
#   ansible.builtin.include_tasks: openbsd.yml # to do
#   when: "'os_openbsd' in group_names"
