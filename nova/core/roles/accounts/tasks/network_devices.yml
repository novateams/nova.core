---
- name: Configure RouterOS accounts...
  include_tasks: routeros.yml
  when: "'os_routeros' in group_names"

- name: Configure VyOS accounts...
  include_tasks: vyos.yml
  when: "'os_vyos' in group_names"

- name: Configure Cisco-IOS accounts...
  include_tasks: ios.yml
  when: "'os_ciscoios' in group_names"

# - name: Configure Fortinet accounts...
#   include_tasks: fortios.yml # to do
#   when: "'os_fortios' in group_names"
