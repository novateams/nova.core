---
- name: Including role {{ pre_role if pre_role is defined else 'omit' }}...
  ansible.builtin.include_role:
    name: "{{ pre_role }}"

- name: Ending play...
  ansible.builtin.meta: end_host
  when: pre_role_stop
