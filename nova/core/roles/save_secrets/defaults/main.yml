---
autogenerated_secret: false # Set to true if you want to overwrite existing secrets
vault_path: "{{ vault_address }}/v1/{{ environment_name }}/data/{{ project_fullname }}"
vault_metadata_path: "{{ vault_address }}/v1/{{ environment_name }}/metadata/{{ project_fullname }}"
kv_path: "{{ project_fullname }}"
